# .env.example - Copy to .env and fill with your secrets (DO NOT commit .env)
# SMTP (Gmail example)
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=mapa.da.vida.25@gmail.com
SPRING_MAIL_PASSWORD=suaw pupn erwk zbqw
SPRING_MAIL_SMTP_AUTH=true
SPRING_MAIL_STARTTLS=true
SPRING_MAIL_SSL_TRUST=smtp.gmail.com

# Optional: enable JavaMail debug output (set to true to see SMTP exchange in logs)
SPRING_MAIL_DEBUG=true

# Example: override server port if you need to run multiple instances
SERVER_PORT=8081

# Notes:
# - For Gmail you must enable 2FA on the account and create an App Password, then put it in SPRING_MAIL_PASSWORD.
# - For local development consider Mailtrap (signup to get credentials) or tools like smtp4dev/mailhog to avoid using a real SMTP server.
# - To load these into your PowerShell session: run .\load-env.ps1 from the project root.

